# Development Dockerfile for frontend (Vue.js)
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production=false
COPY . .
ENV PORT=3000
EXPOSE 3000
CMD ["sh", "-c", "npm run serve -- --host 0.0.0.0 --port $PORT"]
